# This rule is not used by the project thalassa-objectstorage-manager itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to objectstorage.controllers.thalassa.cloud resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: thalassa-objectstorage-manager
    app.kubernetes.io/managed-by: kustomize
  name: bucketaccess-viewer-role
rules:
- apiGroups:
  - objectstorage.controllers.thalassa.cloud
  resources:
  - bucketaccesses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - objectstorage.controllers.thalassa.cloud
  resources:
  - bucketaccesses/status
  verbs:
  - get

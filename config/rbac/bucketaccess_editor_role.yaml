# This rule is not used by the project thalassa-objectstorage-manager itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the objectstorage.controllers.thalassa.cloud.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: thalassa-objectstorage-manager
    app.kubernetes.io/managed-by: kustomize
  name: bucketaccess-editor-role
rules:
- apiGroups:
  - objectstorage.controllers.thalassa.cloud
  resources:
  - bucketaccesses
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - objectstorage.controllers.thalassa.cloud
  resources:
  - bucketaccesses/status
  verbs:
  - get
